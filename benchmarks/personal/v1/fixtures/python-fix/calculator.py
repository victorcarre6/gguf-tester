"""Small intentionally broken project used by the personal command task."""


def divide(total: float, parts: int) -> float:
    """Return an equal share of total for a positive number of parts."""

    return total * parts
